{
  "name": "alpha-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace for the Alpha TypeScript SDK and documentation.",
  "license": "ISC",
  "type": "module",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "dev": "pnpm --filter @averyso/alpha dev",
    "lint": "oxlint --max-warnings=0",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:coverage": "pnpm --filter @averyso/alpha test:coverage",
    "verify": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test:coverage && pnpm build && pnpm pack:check",
    "pack:check": "sh -c 'pnpm --filter @averyso/alpha pack --dry-run; status=$?; rm -f averyso-alpha-*.tgz; exit $status'",
    "docs:dev": "pnpm --filter @averyso/alpha-docs dev",
    "docs:build": "pnpm --filter @averyso/alpha-docs build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm verify && changeset publish",
    "release:publish": "changeset publish",
    "lint-staged": "lint-staged",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "@types/node": "^24.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "oxfmt": "^0.8.0",
    "oxlint": "^1.2.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,jsonc,md,yml,yaml}": [
      "oxfmt"
    ],
    "*.{js,jsx,ts,tsx}": [
      "oxlint"
    ]
  }
}
